<header id="fixed-header">
	<nav class="navbar bg-dark">
		<a class="navbar-brand flex-r items-center" href="/"><i class="material-icons md-24 mr-2">check_box</i>Testing</a>
		<% if (links) { %>
			<button class="toggle" data-target="nav">
				<i class="material-icons md-36">menu</i>
			</button>
		<% } %>
		<div id="nav" class="collapse">
			<ul class="nav">
				<% if (links) { %>
					<% for (let link of links) { %>
						<li><a href="<%= link.route %>"><%= link.name %></a></li>
					<% } %>
				<% } %>
			</ul>
		</div>
	</nav>
</header>